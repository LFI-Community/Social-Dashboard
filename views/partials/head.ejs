<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof pageTitle !== 'undefined' && pageTitle ? pageTitle + ' — ' : '' %>Social Dashboard</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,400;0,600;0,800;0,900;1,600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/static/style.css?v=<%= ASSET_V %>">
</head>
<body>
  <header class="topbar">
    <a class="brand" href="/"><span class="phi">φ</span> Social Dashboard</a>
    <nav>
      <a href="/" class="<%= path === '/' ? 'on' : '' %>">Classement</a>
      <a href="/compare" class="<%= path.startsWith('/compare') ? 'on' : '' %>">Comparer</a>
      <% if (isAdmin) { %>
        <a href="/admin" class="<%= path.startsWith('/admin') ? 'on' : '' %>">Admin</a>
        <form action="/admin/logout" method="post" class="inline"><button class="link">Déconnexion</button></form>
      <% } else { %>
        <a href="/admin/login">Admin</a>
      <% } %>
    </nav>
  </header>
  <main>
